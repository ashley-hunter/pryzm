import { PryzmConfig } from '../config/config';
import { Tree } from '../fs';

/**
 * Load a project from the tree.
 * @param config The configuration for the project.
 * @param fs The file system to load the project from.
 */
export function loadProject(config: PryzmConfig, fs: Tree) {
  //
}
